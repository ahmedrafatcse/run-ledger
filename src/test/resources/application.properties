# Silence Hibernate SQL output
spring.jpa.show-sql=false

# Kill all log output (Spring, Flyway, Testcontainers, etc.)
logging.level.root=OFF

# Remove the Spring Boot ASCII banner
spring.main.banner-mode=off